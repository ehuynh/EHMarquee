#include "Pods-EHMarqueeExample-EHMarquee.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/EHMarquee" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/EHMarquee"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}